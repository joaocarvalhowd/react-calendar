import styled from 'styled-components';

const S = {};

S.Header = styled.header`
  padding: 16px;

  h1 {
    font-weight: 500;
    text-align: center;
    text-transform: uppercase;
    color: #fff;
  }
`;

export default S;
